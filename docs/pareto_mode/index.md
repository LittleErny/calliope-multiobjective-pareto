# Pareto mode

`pareto` mode runs repeated solves to trace a trade-off curve between two objectives.

Supported method: **epsilon-constraint** (`method: epsilon`).

Key config:
- `config.solve.pareto.primary_cost`
- `config.solve.pareto.secondary_cost`
- `config.solve.pareto.epsilons`
